# Machine-written, not source. Everything in this repo is served verbatim at
# https://ee5.net/<path>, so anything that lands here is published -- bytecode
# caches included.
__pycache__/
*.pyc

# Scratch frames pulled out of Live Photo pairs by tools/liveframes.py.
# Machine-written, and this repo publishes whatever it contains.
.liveframes/
